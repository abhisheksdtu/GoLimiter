apiVersion: v1
kind: Service
metadata:
  name: {{ include "golimiter.fullname" . }}
  labels:
    {{- include "golimiter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "golimiter.selectorLabels" . | nindent 4 }}
